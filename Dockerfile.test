FROM sonamines-candidatures-app:latest

COPY requirements-dev.txt /app/requirements-dev.txt
RUN pip install --no-cache-dir --user \
    pytest==7.4.3 \
    pytest-asyncio==0.21.1 \
    pytest-cov==4.1.0

WORKDIR /app
